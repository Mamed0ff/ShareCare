spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/secondlife
    username: postgres
    password: memmedov2004
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true