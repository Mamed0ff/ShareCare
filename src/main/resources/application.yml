spring:
  profiles:
    include:
      - db
      - s3
      - mail
      - file-upload
      - security
      - tg
      - changelog
springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html